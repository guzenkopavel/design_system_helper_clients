---
description: Сверить явные iOS или Android production changes с active package до staging.
---

Use the `reconcile-implementation` skill from
`.agents/skills/reconcile-implementation/SKILL.md` with arguments:
$ARGUMENTS
