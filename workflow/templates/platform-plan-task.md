# task-NNN — <название>

<!-- Goal, context, steps, verification, expected result и out-of-scope писать
по-русски; exact machine fields, IDs, paths, commands и code tokens не переводить. -->

- Layer: domain | data | presentation | infrastructure | tests
- Boundary owner: <связная capability или app-shell composition boundary>
- Engineering scopes: ["<sealed-package-scope>"]
- Depends on: task-NNN | none
- Status: pending
- Evidence: none
- Estimate (ideal): 0.5–2 days
- Read-only context: none | ["<safe-existing-repo-relative-ref>"]
- Paths: proposed: <platform-production-root>/Feature; existing: <platform-production-root>/current/file

`Paths` содержит только writable production paths. Lifecycle artifacts,
`platform-ux.md`, specs, rules и прочие immutable inputs перечислять только в
`Read-only context`.

## Goal
## Inline contract context
## Implementation deliverables

<!-- В каждом пункте оставить самостоятельное prose: после исключения inline
code, paths и IDs — минимум 4 Unicode alphabetic words и 24 letters. Технические
tokens могут уточнять outcome, но не заменять его описание. Не использовать
mixed Cyrillic/Latin lookalikes, zero-width formatting или цепочки односимвольных
фрагментов для набора порога. -->

- <конкретный artifact, behavior, boundary или configuration outcome>
- <конкретный test, integration или observable outcome>

## Steps
## Verification

- Discovered command:
- Watchdog max/stall/output budget for nontrivial checks:
- Applicable rule/check mapping:

## Expected result
## Out of scope
