# Gradle build

Обнаружить wrapper, modules, plugins, variants и существующие tasks. Не считать `build`, detekt или ktlint доступными. Не refresh dependencies, clean или удалять caches без evidence и authority.
