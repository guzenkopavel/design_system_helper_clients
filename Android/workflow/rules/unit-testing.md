# Android unit testing

Тестировать state/repositories/use cases через fakes и deterministic coroutine control. Commands/targets обнаруживаются из проекта; фиксированный coverage threshold не задаётся.
