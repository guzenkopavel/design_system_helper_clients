# Compose

Применяется только при выбранном Compose scope и найденной конфигурации. State hoisting, stable identity, unidirectional events и lifecycle-aware collection; не мигрировать Views без задачи.
