# verification — Android app-shell

## Итог

Fresh Verify для Android `app-shell` выполнен на текущем коде после
implementation commit `5b38448`. Все строки `verification.md` получили
наблюдаемое доказательство и статус `PASS`. Production code, task plan,
contracts, rules и pre-existing evidence во время verifier pass не менялись.

## Runtime и окружение

- Устройство: `Pixel_6(AVD) - 16`.
- ADB: `emulator-5554	device`.
- Package: `Android/specs/app-shell/changes/app-shell`.
- Проверенный consumer launch: `ru.home.sysdevsc/.MainActivity`.

## Команды

### Gradle graph, module build, consumer build и lint

```text
rtk bash workflow/scripts/test-watchdog.sh --max-seconds 420 --stall-seconds 90 --max-output-lines 2200 -- ./Android/gradlew -p Android --no-daemon projects :app-shell:compileDebugKotlin :app-shell:compileDebugUnitTestKotlin :app-shell:compileDebugAndroidTestKotlin app:assembleDebug app:testDebugUnitTest app:lintDebug --console=plain
```

Результат: `BUILD SUCCESSFUL`. Gradle graph содержит только `:app` и
`:app-shell`; `:app-shell` собирает production, unit test и androidTest source;
`:app` проходит assemble, unit tests и lint.

### Deterministic state contract

```text
rtk bash workflow/scripts/test-watchdog.sh --max-seconds 120 --stall-seconds 30 --max-output-lines 220 -- java -cp 'Android/app-shell/build/intermediates/built_in_kotlinc/debugUnitTest/compileDebugUnitTestKotlin/classes:Android/app-shell/build/intermediates/built_in_kotlinc/debug/compileDebugKotlin/classes:/Users/pavel/.gradle/caches/modules-2/files-2.1/org.jetbrains.kotlin/kotlin-stdlib/2.2.10/30de6faa127a4a012db8e71bf1b9c0a99b1402b2/kotlin-stdlib-2.2.10.jar:/Users/pavel/.gradle/caches/modules-2/files-2.1/org.jetbrains.kotlin/kotlin-stdlib-jdk8/2.2.10/80cc9a776a058eeda7053731350bc41af8858a5a/kotlin-stdlib-jdk8-2.2.10.jar' ru.home.sysdevsc.appshell.AppShellStateTestKt
```

Результат: exit code `0`. Проверены начальный выбор `Кейсы`, правило ровно
одного выбранного раздела, переходы по всем трём направлениям и отсутствие
четвёртого состояния.

### Runtime UI integration

```text
rtk bash workflow/scripts/test-watchdog.sh --max-seconds 600 --stall-seconds 120 --max-output-lines 2500 -- ./Android/gradlew -p Android --no-daemon app:connectedDebugAndroidTest --console=plain
```

Результат: `BUILD SUCCESSFUL`. На `Pixel_6(AVD) - 16` выполнены 3 теста.
Проверены launch labels `Кейсы`, `Знания`, `Профиль`, initial selected
semantics `Выбрано`, переход на `Профиль` и нейтральная поверхность выбранного
направления.

### Static inspection

```text
rtk python3 - <<'PY'
from pathlib import Path
import re
...
PY
```

Результат: `VERIFY_STATIC_INSPECTION_PASS`. Проверка подтвердила:

- `settings.gradle.kts` содержит `:app` и `:app-shell`;
- `:app` зависит от `implementation(project(":app-shell"))`;
- `:app` использует Android application plugin, `:app-shell` использует Android
  library plugin;
- `MainActivity` вызывает `AppShell`, не содержит template greeting и
  выполняет только root composition;
- `AppShellState` и `AppShellDestination` не используют Android `Context`,
  `Resources`, Activity, ViewModel, storage или network;
- resource labels равны `Кейсы`, `Знания`, `Профиль`;
- selected semantics строки равны `Выбрано` и `Не выбрано`;
- в shell code/resources нет loading, error, account или data affordances;
- contrast для light/dark soft-blue pairs не ниже `4.5`.

Контраст:

- light primary/onPrimary: `6.44`;
- light primaryContainer/onPrimaryContainer: `10.41`;
- light surface/onSurface: `16.63`;
- dark primary/onPrimary: `7.74`;
- dark primaryContainer/onPrimaryContainer: `7.48`;
- dark surface/onSurface: `14.41`.

## Покрытие contract rows

- `REQ-1`: PASS через runtime labels и Gradle graph.
- `REQ-2`: PASS через deterministic state contract и runtime selected
  semantics.
- `REQ-3`: PASS через runtime transition на `Профиль` и state transition matrix.
- `REQ-4`: PASS через static inspection отсутствия loading/error/account/data
  affordances и нейтральные surface texts.
- `REQ-5`: PASS через resource labels, `stateDescription`, non-color selected
  marker и contrast checks.
- `REQ-6`: PASS через trace fixed labels, order, first selection и observable
  transitions.
- `AC-1`: PASS через runtime labels.
- `AC-2`: PASS через runtime selected semantics `Выбрано`.
- `AC-3`: PASS через state transition matrix и runtime transition evidence.
- `AC-4`: PASS через static inspection shell code/resources.
- `AC-5`: PASS через semantics, labels, non-color cue и light/dark contrast.
- `AND-REQ-1`: PASS через Gradle graph `:app -> :app-shell` и public seam.
- `AND-REQ-2`: PASS через Material 3 shell, resource labels и runtime UI.
- `AND-AC-1`: PASS через discovered Gradle project graph и consumer build.
- `AND-AC-2`: PASS через deterministic state test и Compose runtime test.
- `AND-AC-3`: PASS через runtime/inspection evidence для labels, selected
  semantics, appearance, accessible on-colors и soft-blue fallback.

## Modularity verification

- Dependency graph: PASS — Gradle graph содержит `:app` и `:app-shell`, а
  `:app` зависит от `:app-shell`.
- Public API and visibility: PASS — consumer использует `AppShell`,
  `AppShellState` и `select(...)`; shell не зависит от `:app`.
- Module-level tests: PASS — `:app-shell:compileDebugUnitTestKotlin` и
  deterministic state contract прошли.
- Consumer integration and build: PASS — `app:assembleDebug`,
  `app:testDebugUnitTest`, `app:lintDebug` и `app:connectedDebugAndroidTest`
  прошли.
- App-shell allowlist: PASS — `:app` выполняет entry point, lifecycle и root
  composition; feature UI/state остаётся в `:app-shell`.

## Native UX verification

Material 3, `MaterialTheme`, navigation components, русские resource labels,
selected semantics, non-color cue, light/dark color schemes, accessible
on-colors и soft-blue fallback проверены runtime и static inspection. Dynamic
color не включён и не переопределён consumer wiring.
