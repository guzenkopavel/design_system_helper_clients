interface:
  display_name: "Deep Code Review"
  short_description: "Read-only review, feedback, bug или security"
  default_prompt: "Use $deep-code-review with an explicit mode and required platform identity; report findings without edits."
policy:
  allow_implicit_invocation: false
