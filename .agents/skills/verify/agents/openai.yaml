interface:
  display_name: "Verify"
  short_description: "Проверить scoped platform change"
  default_prompt: "Use $verify with a platform, feature, and optional change ID; independently derive the scoped verification matrix."
policy:
  allow_implicit_invocation: false
