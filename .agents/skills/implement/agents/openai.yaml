interface:
  display_name: "Implement"
  short_description: "Выполнить scoped platform tasks"
  default_prompt: "Use $implement with a platform, feature, optional change ID, and task selection; apply exact rule profiles and bounded checks."
policy:
  allow_implicit_invocation: false
