interface:
  display_name: "Propose"
  short_description: "Создать scoped platform package"
  default_prompt: "Use $propose with a platform, feature, and optional change ID; derive engineering scopes from repository evidence."
policy:
  allow_implicit_invocation: false
