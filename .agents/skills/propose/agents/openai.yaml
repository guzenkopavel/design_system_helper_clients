interface:
  display_name: "Propose"
  short_description: "Создать change-aware platform package"
  default_prompt: "Use $propose with a platform, feature, and optional change ID."
policy:
  allow_implicit_invocation: false
