interface:
  display_name: "Plan"
  short_description: "Создать change-aware execution plan"
  default_prompt: "Use $plan with a platform, feature, and optional change ID."
policy:
  allow_implicit_invocation: false
