# Verify evidence — tests — 20260717T112843Z

- Command: `swift test --package-path iOS/MyProfileFeature`
- Result: `PASS`
- Observation: package tests прошли 16 тестов без failures; manifest обрабатывает
  `Resources/Localizable.ru.strings`, поэтому предупреждение о необработанном
  ресурсе не воспроизводится.

- Command: `bash workflow/scripts/test-watchdog.sh --max-seconds 300 --stall-seconds 60 --max-output-lines 40000 -- xcodebuild -project iOS/SysDevScen/SysDevScen.xcodeproj -scheme SysDevScen -destination 'platform=iOS Simulator,name=iPhone 17' -parallel-testing-enabled NO -only-testing:SysDevScenTests/AppShellStateTests test`
- Result: `PASS`
- Observation: focused state tests подтвердили, что app shell использует
  `MyProfileFeatureFactory`, получает preview session client из feature package и
  не содержит профильные `AuthSessionRequest` cases.

- Command: `bash workflow/scripts/test-watchdog.sh --max-seconds 600 --stall-seconds 90 --max-output-lines 60000 -- xcodebuild -project iOS/SysDevScen/SysDevScen.xcodeproj -scheme SysDevScen -destination 'platform=iOS Simulator,name=iPhone 17' -parallel-testing-enabled NO test`
- Result: `PASS`
- Observation: full simulator suite прошёл 18 UI tests и 5 app-shell state tests
  без failures; result bundle:
  `/Users/pavel/Library/Developer/Xcode/DerivedData/SysDevScen-bdowkpzdxhavojgpkycvjqozgfjp/Logs/Test/Test-SysDevScen-2026.07.17_18-24-41-+0700.xcresult`.

Покрытие: package tests закрывают pagination, duplicate-load guard,
cancellation, error mapping, invalid session, logout failure и presentation
environment branches. UI tests закрывают content, empty history, history error,
invalid session, logout и logout failure outcomes.
