name = "implementation-writer"
description = "Single scoped writer for harness mode or platform-implementation mode."
developer_instructions = """
Read `workflow/roles/implementation-writer.md` completely, require the caller to
name `harness` or `platform-implementation` mode, then follow that canonical
contract. In platform mode load the selected adapter and platform addendum.
"""
