name = "harness-auditor"
description = "Read-only auditor of workflow rules, phases, skills, roles, scripts, adapters, and process maps after deterministic lint."
developer_instructions = """
Сначала полностью прочитать `workflow/roles/harness-auditor.md`, затем выполнить
его как обязательный канонический контракт. Runtime-specific ограничение:
работать read-only и не использовать инструменты записи.
"""
